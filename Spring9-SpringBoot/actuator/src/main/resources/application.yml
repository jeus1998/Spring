management:
  # 활성화
  endpoint:
    shutdown:
      enabled: true
    health:
  #    show-components: always
        show-details: always
  endpoints:
  # 노출
    web:
      exposure:
        include: "*"