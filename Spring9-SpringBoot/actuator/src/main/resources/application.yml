management:
  # info: management 바로 아래서 시작
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  # 활성화
  endpoint:
    shutdown:
      enabled: true
    health:
  #    show-components: always
        show-details: always
  endpoints:
  # 노출
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: jeu
